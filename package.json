{
  "name": "asus_router_downloadmaster_telegram_bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "build:clean": "rimraf dist",
    "build": "npm run build:clean && tsc",
    "dev": "npm run build && node dist/index.js",
    "build:function": "npm run build && cp package.json dist/ && cd ./dist && cross-zip . dist && mv dist.zip ../"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "telegraf": "^4.16.3",
    "ts-dedent": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "cross-zip-cli": "^1.0.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.5"
  }
}
